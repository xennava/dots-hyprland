$lock_cmd = hyprctl dispatch 'hl.dsp.global("quickshell:lock")' & pidof qs quickshell hyprlock || hyprlock
# $lock_cmd = pidof hyprlock || hyprlock
$suspend_cmd = systemctl suspend || loginctl suspend

general {
    lock_cmd = $lock_cmd
    before_sleep_cmd = loginctl lock-session
    # after_sleep_cmd = hyprctl dispatch 'hl.dsp.global("quickshell:lock")'
    inhibit_sleep = 3
}

listener {
    timeout = 300 # 5mins
    on-timeout = loginctl lock-session
}

listener {
    timeout = 600 # 10mins
<<<<<<< HEAD
    on-timeout = hyprctl dispatch 'hl.dsp.dpms({ action=off, monitor=current })'
    on-resume = hyprctl dispatch 'hl.dsp.dpms({ action=on, monitor=current })'
=======
    on-timeout = hyprctl dispatch 'hl.dsp.dpms(false)'
    on-resume = hyprctl dispatch 'hl.dsp.dpms(true)'
>>>>>>> ce809512 (fix: hypridle dispatch commands for Lua-based Hyprland)
}

listener {
    timeout = 900 # 15mins
    on-timeout = $suspend_cmd
}
